% ------------------------------------------------------------
% Language, character encoding, and quotation marks
% ------------------------------------------------------------
\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{csquotes}

\ifdefined\XeTeXversion
  \usepackage{fontspec}
\fi

% ------------------------------------------------------------
% Page layout: A4, 2 cm margins
% ------------------------------------------------------------
\usepackage{geometry}
\geometry{
  top=2cm,
  bottom=2cm,
  left=2cm,
  right=2cm
}

% ------------------------------------------------------------
% Line spacing and microtypography
% ------------------------------------------------------------
\usepackage{setspace}
\onehalfspacing
\usepackage{microtype}
% English compounds and inline identifiers need slightly more line-breaking
% flexibility than the German source to avoid protruding into the margins.
\setlength{\emergencystretch}{3em}

% ------------------------------------------------------------
% Font: TeX Gyre Heros as an Arial-like typeface
% ------------------------------------------------------------
\ifdefined\XeTeXversion
  \setsansfont{TeX Gyre Heros}[
    UprightFont=TeX Gyre Heros,
    BoldFont=TeX Gyre Heros Bold,
    ItalicFont=TeX Gyre Heros Italic,
    BoldItalicFont=TeX Gyre Heros Bold Italic
  ]
\else
  \usepackage{tgheros}
\fi
\renewcommand{\familydefault}{\sfdefault}

% ------------------------------------------------------------
% Heading formatting
% ------------------------------------------------------------
\usepackage{titlesec}

\titleformat{\section}
  {\bfseries\fontsize{12pt}{14pt}\selectfont}
  {\thesection}
  {1em}
  {}

\titleformat{\subsection}
  {\bfseries\fontsize{12pt}{14pt}\selectfont}
  {\thesubsection}
  {1em}
  {}

\titleformat{\subsubsection}
  {\bfseries\fontsize{12pt}{14pt}\selectfont}
  {\thesubsubsection}
  {1em}
  {}

\setcounter{secnumdepth}{3}
\setcounter{tocdepth}{3}

% ------------------------------------------------------------
% Paragraphs and footnotes
% ------------------------------------------------------------
\setlength{\parskip}{6pt}
\setlength{\parindent}{0pt}

\makeatletter
\renewcommand\footnotesize{\@setfontsize\footnotesize{10pt}{12pt}}
\makeatother

% ------------------------------------------------------------
% Page numbers: centered at the bottom, no header
% ------------------------------------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}

% ------------------------------------------------------------
% Graphics, tables, and code-based figures
% ------------------------------------------------------------
\usepackage{graphicx}
\usepackage{xcolor}
\ifbeginnerstatements
  \usepackage{tcolorbox}
\fi
\usepackage{tikz}
\usetikzlibrary{
  arrows.meta,
  backgrounds,
  calc,
  fit,
  matrix,
  positioning,
  shapes.geometric
}

% Consistent colors for the case-study figures
\definecolor{caseBlue}{HTML}{174A7E}
\definecolor{caseGreen}{HTML}{5A8F3D}
\definecolor{caseLight}{HTML}{F4F8FB}
\definecolor{caseLine}{HTML}{A9B4BF}

% Optional, unobtrusive explanations for readers new to the technology
\ifbeginnerstatements
  \definecolor{beginnerBackground}{HTML}{F3F3F3}
  \definecolor{beginnerBorder}{HTML}{B8B8B8}

  \newtcolorbox{beginnerstatement}[1][In simple terms]{
    colback=beginnerBackground,
    colframe=beginnerBorder,
    colbacktitle=beginnerBackground,
    coltitle=black,
    coltext=black,
    boxrule=0.6pt,
    arc=2mm,
    outer arc=2mm,
    left=2.5mm,
    right=2.5mm,
    top=2mm,
    bottom=2mm,
    before skip=10pt plus 2pt minus 2pt,
    after skip=10pt plus 2pt minus 2pt,
    fonttitle=\bfseries,
    fontupper=\small,
    title={#1}
  }
\fi

\tikzset{
  casePrimary/.style={
    draw=caseBlue,
    fill=caseBlue,
    text=white,
    rounded corners=3pt,
    align=center,
    font=\sffamily\bfseries\scriptsize,
    minimum height=8mm,
    inner sep=4pt
  },
  caseBox/.style={
    draw=caseBlue,
    fill=white,
    text=caseBlue,
    rounded corners=3pt,
    align=center,
    font=\sffamily\scriptsize,
    minimum height=8mm,
    inner sep=4pt
  },
  caseOptional/.style={
    draw=caseGreen,
    fill=caseGreen!10,
    text=caseGreen!55!black,
    rounded corners=3pt,
    align=center,
    font=\sffamily\scriptsize,
    minimum height=8mm,
    inner sep=4pt
  },
  caseGroup/.style={
    draw=caseLine,
    fill=caseLight,
    rounded corners=5pt,
    inner sep=6pt
  },
  caseFlow/.style={
    -{Latex[length=2.6mm,width=1.8mm]},
    draw=caseBlue,
    line width=0.9pt
  },
  caseSecondaryFlow/.style={
    -{Latex[length=2.4mm,width=1.7mm]},
    draw=caseLine,
    line width=0.8pt,
    dashed
  }
}

\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{float}
\usepackage{threeparttable}

% ------------------------------------------------------------
% Hyperlinks
% ------------------------------------------------------------
\usepackage{xurl}
\usepackage[hidelinks]{hyperref}

% ------------------------------------------------------------
% Bibliography management with biblatex-apa
% ------------------------------------------------------------
\usepackage[
  style=apa,
  backend=biber,
  sorting=nyt,
  giveninits=true,
  maxcitenames=2,
  maxbibnames=20,
  doi=true,
  url=true,
  isbn=true
]{biblatex}

\DeclareLanguageMapping{english}{english-apa}

\DefineBibliographyStrings{english}{
  nodate = {n\adddot d\adddot}
}

\AtBeginBibliography{\raggedright\Urlmuskip=0mu plus 1mu}

\addbibresource{references.bib}

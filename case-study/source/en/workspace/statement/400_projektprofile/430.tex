\begin{beginnerstatement}[In simple terms: web-cloud]

\texttt{web-cloud} is a browser application with a separate FastAPI backend.
The profile also provides a provider-agnostic foundation for deployment. A
PostgreSQL database is added only through the separately selected capability.

\end{beginnerstatement}

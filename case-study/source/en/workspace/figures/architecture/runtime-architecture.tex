\begin{figure}[H]
  \centering
  \resizebox{0.88\textwidth}{!}{%
  \begin{tikzpicture}[node distance=8mm and 34mm]
    \node[casePrimary,minimum width=34mm] (browser) {Browser};
    \node[casePrimary,minimum width=34mm,right=of browser] (desktop) {Tauri webview};
    \node[caseBox,minimum width=70mm,below=17mm of $(browser)!0.5!(desktop)$] (frontend)
      {one shared Vite/TypeScript build};
    \node[caseOptional,minimum width=48mm,below=of frontend] (api)
      {FastAPI service\\backend-capable profiles only};
    \node[caseOptional,minimum width=48mm,below=of api] (postgres)
      {SQLAlchemy + Psycopg\\PostgreSQL optional};

    \draw[caseFlow] (browser) -- node[pos=0.42,left,font=\sffamily\tiny] {loads} (frontend);
    \draw[caseFlow] (desktop) -- node[pos=0.42,right,font=\sffamily\tiny] {hosts} (frontend);
    \draw[caseSecondaryFlow] (frontend) -- node[right,font=\sffamily\tiny] {HTTP /api} (api);
    \draw[caseSecondaryFlow] (api) -- node[right,font=\sffamily\tiny] {server-side} (postgres);

    \begin{scope}[on background layer]
      \node[caseGroup,fit=(browser)(desktop)(postgres),
        label={[font=\sffamily\scriptsize,text=black!60]below:component set depends on the resolved profile}] {};
    \end{scope}
  \end{tikzpicture}}
  \caption{Profile-dependent runtime architecture}
  \label{fig:runtime-architecture}
  \smallskip
  {\footnotesize Source: Author's own illustration based on
  \textcite{kleiveist2026architecture} and \textcite{kleiveist2026profiles}.\par}
\end{figure}
